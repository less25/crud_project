<% content_for :title, "Nueva Categoría" %>

<h1 class="mb-4 text-success">Nueva Categoría</h1>

<div class="card mb-4 shadow-sm" style="background-color: #f8d7da; border-radius: 12px;">
  <div class="card-body">
    <%= render "form", categorium: @categorium %>
  </div>
</div>

<div class="d-flex">
  <%= link_to "Regresar a categorías", categoria_path, class: "btn btn-secondary" %>
</div>
